spring:
  rabbitmq:
    listener:
      simple:
        auto-startup: false

management:
  health:
    rabbit:
      enabled: false
    redis:
      enabled: false

rescue-lab:
  idempotency:
    cleanup-initial-delay: PT24H
  events:
    consumer-enabled: false
    publisher:
      enabled: false
